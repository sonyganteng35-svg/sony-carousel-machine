name: Weekly Research Trigger

on:
  schedule:
    - cron: "0 12 * * 0"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Sony Carousel Machine
        run: echo "SONY CAROUSEL MACHINE RUNNING"
